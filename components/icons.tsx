/**
 * Shared inline icons.
 *
 * LinkedInIcon: a single-path LinkedIn glyph used in the hero, contact, about,
 * and testimonial sections. Kept as inline SVG (not lucide) so it renders at
 * any size with currentColor and no extra dependency.
 */

export function LinkedInIcon({ size = 16 }: { size?: number }) {
  return (
    <svg width={size} height={size} viewBox="0 0 24 24" fill="currentColor" aria-hidden>
      <path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z" />
      <rect x="2" y="9" width="4" height="12" />
      <circle cx="4" cy="4" r="2" />
    </svg>
  );
}
