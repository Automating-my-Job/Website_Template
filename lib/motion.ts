/**
 * Shared motion tokens.
 *
 * EASE is the site's canonical entrance easing (a custom cubic-bezier). It is
 * used by every scroll/entrance animation, so it lives here as the single
 * source of truth: tune the feel of the whole site by editing one value.
 */
export const EASE = [0.22, 1, 0.36, 1] as [number, number, number, number];
